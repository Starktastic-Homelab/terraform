{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "matchDatasources": ["terraform-provider"],
      "matchDepTypes": ["required_provider"],
      "matchPackageNames": ["telmate/proxmox"],
      "rangeStrategy": "bump",
      "enabled": true,
      "versioning": "loose"
    }
  ]
}
